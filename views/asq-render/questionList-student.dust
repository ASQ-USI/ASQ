{>"questionForm-student.dust"/}

{<questionContent}
  <ol>
  {#questionOptions}
    <li class="{classList}" >
      <label class="{question.formButtonType}">
        <input type="{formButtonType}" name="{htmlId}" value="{$idx}"> {text|s}
      </label>
    </li>{~n}
  {/questionOptions}
</ol>
{/questionContent}